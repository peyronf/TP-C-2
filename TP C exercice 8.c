#include <stdio.h>
void main()
{
    char mois;
    int jour,annee;
    printf("Indiquer la date en format jjmmaa : ");
    scanf("%d, %c,%d", jour, mois, annee);
    /* utiliser un switch case pour le mois, et revoyer les valeurs
    entière pour le jour et l'annee */
}
